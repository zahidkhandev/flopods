/**
 * Document Errors Barrel Export
 *
 * @description Centralized export for all document processing error classes.
 * Provides clean imports for error handling.
 *
 * @module v1/documents/errors
 *
 * @example
 * ```
 * import {
 *   YouTubeError,
 *   YouTubeInvalidURLError,
 *   YouTubeVideoNotFoundError,
 *   YouTubeQuotaExceededError,
 * } from '../errors';
 * ```
 */

export * from './youtube-errors';
